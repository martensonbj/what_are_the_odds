<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="row">
      <div class="navbar-header">
        <% if current_user %>
        <%= link_to 'What Are The Odds?', dashboard_path, class: 'navbar-brand' %>
        <% else %>
        <%= link_to 'What Are The Odds?', root_path, class: 'navbar-brand' %>
        <% end %>
      </div>
      <div id="navbar">
        <ul class="nav navbar-nav pull-right">
          <% if current_user %>
          <li><%= link_to 'Challenge History', challenges_path %></li>
          <li><%= link_to 'Logout', logout_path, method: :delete %></li>
          <li><%= image_tag current_user.picture, alt: current_user.name %></li>
          <% else %>
          <li><%= link_to 'Log In', '/auth/facebook' %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
